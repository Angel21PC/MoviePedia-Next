import React, { useState } from "react";

export interface TextEditor {
  onSubmit?: any;
}

const TextEditor: React.SFC<TextEditor> = ({ onSubmit }) => {
  return <></>;
};

export default TextEditor;
